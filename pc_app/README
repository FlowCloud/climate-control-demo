Climate control app

Need these python2.7 packages for running this app
	PySide > 1.2.1
	matplotlib >= 1.3.1
	enum34 >= 1.0.4
	xmltodict >= 0.9.2
	netifaces >= 0.8

Run the app using these command
    - Admin app
    #python climate_control/admin
    - Display app
    #python climate_control/display

For changing log level
    #python climate_control/admin --log-level=DEBUG

For knowing the versions
    #python climate_control/admin --version

